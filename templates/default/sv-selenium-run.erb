#!/bin/sh
envdir=$PWD/env
exec 2>&1 \
chpst -e $envdir -u <%= node['selenium']['server']['user'] %> java -jar <%= File.join(node['selenium']['server']['installpath'], 'selenium-server-standalone.jar') %> 2>&1